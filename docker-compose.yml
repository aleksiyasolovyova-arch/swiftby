services:
  db:
    image: postgres:17.2-alpine
    restart: always
    environment:
      POSTGRES_DB: 'swift_by_db'
      POSTGRES_USER: 'admin'
      POSTGRES_PASSWORD: 'admin'
    ports:
      - '5435:5432'
    volumes:
      - db-data:/var/lib/postgresql/data
volumes:
  db-data:
